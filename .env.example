# 複製這個檔案為 .env 並填入實際值。
# 這個範本會進版控（.gitignore 裡有 `!.env.example` 的例外），.env 不會。
#
# ⚠️ **這裡放的每一個變數都會被送到瀏覽器。** 見下面「為什麼只有 public」那一段 ——
# 前端沒有「只有伺服器看得到」的秘密可言，API 金鑰之類的東西不要放這裡。

# ------------------------------------------------------------
# 後端 API 的位址（Ch16）
# ------------------------------------------------------------
# 對應 nuxt.config.ts 的 runtimeConfig.public.apiBase。
#
# **命名規則不是自己取的，是 Nuxt 的約定**：
#
#     runtimeConfig.public.apiBase   ←   NUXT_PUBLIC_API_BASE
#     └─ 固定前綴 NUXT_ ─┘ └ public ┘   └ camelCase 拆成大寫底線 ┘
#
# 名字拼錯不會有任何錯誤訊息 —— Nuxt 只是沒認出它，然後靜靜地用
# nuxt.config.ts 裡的預設值（http://localhost:3100）。所以改完要驗證，
# 不要假設它生效了（驗法見 docs/前端分層慣例.md）。
#
# **不設也能跑**，這一點跟後端相反：後端的 CORS_ORIGIN 拿不到會讓應用起不來，
# 這裡拿不到只是退回預設值。差別在於「預設值是不是安全的」——
# 後端退回 localhost 等於線上整站不能用（而且症狀會騙人），
# 前端退回 localhost:3100 在本機開發正是你要的。
NUXT_PUBLIC_API_BASE=http://localhost:3100

# ------------------------------------------------------------
# 為什麼只有 public，沒有伺服器端的私密變數
# ------------------------------------------------------------
# Nuxt 的 runtimeConfig 分兩層：
#
#   runtimeConfig.xxx          只有伺服器端讀得到（NUXT_XXX）
#   runtimeConfig.public.xxx   伺服器與瀏覽器都讀得到（NUXT_PUBLIC_XXX）
#
# 這個專案的前端**沒有任何秘密**：token 是使用者自己登入拿到的、存在他自己的 cookie，
# 而所有授權判斷都在後端。前端從頭到尾只是一個「幫使用者組請求」的殼。
#
# 哪天需要私密變數了（例如伺服器端要用的第三方金鑰），記得它**不能**放 public ——
# public 的內容會被序列化進 HTML 送到瀏覽器，等於公開。
#
# ------------------------------------------------------------
# SSR 與靜態產出的差別（會影響這個檔案什麼時候被讀）
# ------------------------------------------------------------
#   pnpm build    → SSR 產物。runtimeConfig 是**執行期**讀環境變數的，
#                   所以同一份 build 可以部署到不同環境，改變數不必重 build。
#   pnpm generate → 靜態產物。沒有伺服器可以在執行期讀，值在 **build 當下**就寫死了。
#
# 這個專案走 SSR（Ch18 部署時要記得：在平台上設 NUXT_PUBLIC_API_BASE，
# 而不是把它 build 進去）。
